{{>layout/header}}


<script>
var new_popup = window.open('test.html', 'width=300px; height=300px;');
new_popup.close();

function hamsu()
{
    alert("내담자 정보 수정 완료")

}
function hamsu2()
{
    alert("내담자 정보 삭제 완료")

}

	function check_onclick(){
		theForm=document.frm1;

		if(theForm.input1.value=="" || theForm.input2.value=="" || theForm.input3.value=="" ){
			alert("필수 입력란이 비었습니다. 확인해 주세요.");
		}
		else{
			alert("필수 입력란을 모두 입력하셨습니다.");
		}
	}

</script>






<h1 >내담자 정보 수정</h1>

<div class="col-md-12">
    <div class="col-md-4">
        <form>
            <div class="form-group">
                <label for="title"  >내담자번호</label>
                <input type="text"   class="form-control" id="id" value="{{client.id}}" readonly>
            </div>
            <div class="form-group">
                <label for="name" >이름</label>
                <input type="text"  class="form-control" id="name" value="{{client.name}}">
            </div>
            <div class="form-group">
                <label for="age" > 나이 </label>
                <input type="text"  class="form-control" id="age" value="{{client.age}}">
            </div>
            <div class="form-group">
                <label for="job" > 직업 </label>
                <input type="text" class="form-control"  id="job" value="{{client.job}}">
            </div>
        </form>
        <a href="/client/index"  role="button" onclick="window.close();" class="btn btn-secondary">취소</a>
        <button type="button"   onclick()="hamsu();" class="btn btn-primary" id="client-btn-update">수정 완료</button>
        <button type="button"  onclick()="hamsu2();" class="btn btn-danger" id="client-btn-delete">삭제</button>
    </div>
</div>

{{>layout/footer}}